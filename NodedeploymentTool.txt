https://devcenter.heroku.com/articles/heroku-cli

Heroku

Steps: sudo snap install --classic heroku
2   -   login using cli by heroko login and press any key


ls -a -l ~/.ssh

ssh-keygen -t rsa -b 4096 -C "guptarishi64@gmail.com"

ls -a -l ~/.ssh

eval "$(ssh-agent -s)"

ssh-add ~/.ssh/id_rsa

then go to github create new repository using ssh and add a remote brnch
i.e: git remote add origin git@github.com:rishi0395/node-weather-website.git

goto github, settings, SSH and GPG keys, and click on new ssh key
give key like "work laptop" {anything will be fine and easy to identify}
for key: type following command on the terminal
cat ~/.ssh/id_rsa.pub

the output will have to copy and paste into the "key" of the github page and click add key

ssh -T git@github.com //to check weather the ssh is configured correctly type the given command

git push -u origin master


NOW will deploy the node to Heroku

heroku keys:add
and hit y


//create heroku project
heroku create gupta-weather-application

two links will generate

then, got to package.json and add start script to type {"start": "node src/app.js"}


